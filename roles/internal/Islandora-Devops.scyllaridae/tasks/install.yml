---
- name: Install requisite packages
  package:
    name: "{{ item }}"
    state: present
  with_items: "{{ scyllaridae_packages }}"
  become: true

- name: Create scyllaridae user
  user:
    name: "{{ scyllaridae_user }}"
    home: "/home/{{ scyllaridae_user }}"
    shell: /bin/bash
    system: true
  become: true

- name: Ensure scyllaridae directory exists
  file:
    path: "{{ scyllaridae_install_dir }}"
    state: directory
    owner: "{{ scyllaridae_user }}"
    group: "{{ scyllaridae_user }}"
    mode: "0755"
  become: true

- name: Select scyllaridae release architecture
  set_fact:
    scyllaridae_release_arch: "{{ scyllaridae_release_arch_map[ansible_architecture] | default(ansible_architecture) }}"
    scyllaridae_expected_file_arch: "{{ scyllaridae_file_arch_map[ansible_architecture] | default(ansible_architecture) }}"

- name: Check if scyllaridae binary exists
  stat:
    path: "{{ scyllaridae_install_dir }}/scyllaridae"
  register: scyllaridae_binary
  become: true

- name: Check scyllaridae binary architecture
  command: "file {{ scyllaridae_install_dir }}/scyllaridae"
  register: scyllaridae_binary_file
  changed_when: false
  failed_when: false
  become: true
  when: scyllaridae_binary.stat.exists

- name: Determine whether scyllaridae needs installation
  set_fact:
    scyllaridae_install_required: >-
      {{
        (not scyllaridae_binary.stat.exists) or
        (scyllaridae_expected_file_arch not in (scyllaridae_binary_file.stdout | default('')))
      }}

- name: Remove stale scyllaridae binary
  file:
    path: "{{ scyllaridae_install_dir }}/scyllaridae"
    state: absent
  become: true
  when:
    - scyllaridae_binary.stat.exists
    - scyllaridae_install_required

- name: Download scyllaridae tar
  get_url:
    url: "https://github.com/Islandora/scyllaridae/releases/download/{{ scyllaridae_version }}/scyllaridae_Linux_{{ scyllaridae_release_arch }}.tar.gz"
    dest: "/tmp/scyllaridae-{{ scyllaridae_version }}.tar.gz"
    mode: "0644"
    force: true
  become: true
  when: scyllaridae_install_required

- name: Extract scyllaridae binary
  unarchive:
    src: "/tmp/scyllaridae-{{ scyllaridae_version }}.tar.gz"
    dest: "{{ scyllaridae_install_dir }}"
    remote_src: true
    owner: "{{ scyllaridae_user }}"
    group: "{{ scyllaridae_user }}"
    mode: "0755"
  become: true
  when: scyllaridae_install_required
  notify: restart scyllaridae services

- name: Clean up downloaded tar
  file:
    path: "/tmp/scyllaridae-{{ scyllaridae_version }}.tar.gz"
    state: absent
  become: true
  when: scyllaridae_install_required

- name: Create configuration directories for services
  file:
    path: "{{ scyllaridae_install_dir }}/{{ item }}"
    state: directory
    owner: "{{ scyllaridae_user }}"
    group: "{{ scyllaridae_user }}"
    mode: "0755"
  with_items: "{{ scyllaridae_services }}"
  become: true

- name: Install service configuration files
  template:
    src: "{{ item }}/scyllaridae.yml"
    dest: "{{ scyllaridae_install_dir }}/{{ item }}/scyllaridae.yml"
    owner: "{{ scyllaridae_user }}"
    group: "{{ scyllaridae_user }}"
    mode: "0644"
  with_items: "{{ scyllaridae_services }}"
  become: true

- name: Check if cmd.sh exists for services
  stat:
    path: "{{ role_path }}/templates/{{ item }}/cmd.sh"
  register: cmd_script_check
  with_items: "{{ scyllaridae_services }}"
  delegate_to: localhost
  become: false

- name: Install service command scripts
  template:
    src: "{{ item.item }}/cmd.sh"
    dest: "{{ scyllaridae_install_dir }}/{{ item.item }}/cmd.sh"
    owner: "{{ scyllaridae_user }}"
    group: "{{ scyllaridae_user }}"
    mode: "0755"
  with_items: "{{ cmd_script_check.results }}"
  when: item.stat.exists
  become: true

- name: Create systemd service files
  template:
    src: "systemd/scyllaridae.service.j2"
    dest: "/etc/systemd/system/scyllaridae-{{ item }}.service"
    mode: "0644"
  with_items: "{{ scyllaridae_services }}"
  become: true
  notify: restart scyllaridae services

- name: Enable and start scyllaridae services
  systemd:
    name: "scyllaridae-{{ item }}"
    enabled: true
    state: started
    daemon_reload: true
  with_items: "{{ scyllaridae_services }}"
  become: true

- name: Create Islandora log dir
  file:
    path: "{{ scyllaridae_log_directory }}"
    state: directory
    owner: "{{ apache_user }}"
    group: "{{ apache_user }}"
    mode: "0775"
  become: true
