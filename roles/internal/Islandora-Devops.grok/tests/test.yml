---
- hosts: all

  pre_tasks:
    - name: Install dependancies (apt)
      apt:
        update_cache: yes
        cache_valid_time: 600
        name: "{{ item }}"
      with_items:
        - git
        - build-essential

  roles:
    - role_under_test
